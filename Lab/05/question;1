#include <stdio.h>

int main() {
   int n;
   int ans;
   printf("enter the number:");
   scanf("%d",&n);
   ans = n % 3;
   if (ans == 0) {
   printf("the number is multiply by 3");
   }
   else if (ans != 0) {
   printf("the number is not multiply by 3");
   }
return 0;
}
